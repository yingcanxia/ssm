<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.canying.dao.UserMapper">
    <!-- <cache type="cn.canying.redis.RedisCache" /> -->
    <!-- 上边的cache如果不只是将会走.redis报的RedisCache类进行序列化与缓存 -->
    <!-- <resultMap id="BaseResultMap" type="cn.canying.bean.User">
        <id column="ID" jdbcType="INTEGER" property="id" />
        <result column="USERNAME" jdbcType="VARCHAR" property="username" />
        <result column="PASSWORD" jdbcType="VARCHAR" property="password" />
        <result column="AGE" jdbcType="INTEGER" property="age" />
    </resultMap> -->
    <select id="test" parameterType="pd" resultType="cn.canying.bean.User">
    	select * from user
    </select>
</mapper>